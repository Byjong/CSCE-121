#include <iostream>
#include "mystery_formula.h"

using std::cout, std::endl;

int main() {
  bool w = 0;
  bool x = 1;
  bool y = 1;
  bool z = 1;
  // if w,x,y,z are set correctly, you will pass the test.
  f(w, x, y, z);
}